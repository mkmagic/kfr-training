# 04 — DFT / FFT Basics

## Goals
- Create a `dft_plan` and run `execute`.
- Use the temp buffer and inspect magnitude.

## Files
- `main.cpp`
- Reference: `include/kfr/dft/fft.hpp`, `examples/dft.cpp`.

## Exercises
1. Change the FFT size to 256 and re-run.
2. Zero-pad the input to double the size.
3. Compare magnitude before and after scaling by `size`.
